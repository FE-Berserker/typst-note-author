// 示例笔记之一，同时也是用法文档：模板提供的元素在这里逐个排过一遍。
#import "../note.typ": *
#import "../figstyle.typ": *

#note-header(
  title: "笔记模板用法",
  date: "2026-09-20",
  tags: ("Typst", "模板"),
  status: "持续更新",
  source: "https://typst.app/docs/",
  summary: "这篇笔记本身是用法文档：把模板提供的元素逐个排一遍。写新笔记时照抄即可，不需要另查文档。",
)

== 笔记头

每篇笔记的第一行都是 `#note-header(…)`。它除了排版笔记头，还顺手做三件事：让这篇笔记另起一页、埋一枚供汇总册目录使用的隐形标题、把定理环境的编号归零。所以别跳过它直接写正文。

#pnote(note: [状态圆点的颜色在 `note.typ` 的 `status-colors` 里改，默认有草稿、待复习、待验证、持续更新、已定稿五种。])[
  标题之外有五个可选字段：日期、标签、状态、来源、摘要。标签写进笔记头比写在正文里更容易检索，状态用来标记这篇笔记的成熟度——笔记是会长的东西，草稿和定稿不该长一个样。
]

来源字段值得单独说一句：笔记多半整理自别处，出处和笔记本身一样重要，留一个可点的链接，半年后回来能立刻找到原文。

== 旁注

右侧那条空栏是给旁注的。一段话要同时说两种声音——正文说「这件事是怎样的」，旁注说「我为什么信」，用 `#pnote` 把两者并排放：

#pnote(note: [没有旁注的段落就是普通段落，断行宽度与带旁注的段落一样，版面不会一页宽一页窄。])[
  一本书的正文只能有一个声音，而笔记里同时存在「作者说的」「我理解的」「我怀疑的」三种声音。都塞进正文，读者（多半是你自己）就会在注解和正文之间反复跳；排到边上，正文可以一口气读下去，需要时再往右看一眼。
]

#pnote-warn(note: [一条旁注配一个段落。同一段落想要两条旁注，把段落拆成两段。])[
  旁注锚在*段落*上，不锚在句子上：它的顶边对齐所在段落的顶边。`place` 写在段落里时定位基准是外层容器而不是当前行，硬做行级锚定会让同页几条旁注叠在一起，所以 `#pnote` 走了并排这条路。
]

#pnote-tip(note: [旁注字号比正文小两号（8.5pt），颜色也浅一档，所以即使排得密也不会抢正文。])[
  旁注比段落长的时候会往下伸出去，不影响正文断行，也不会把后面的内容挤走。
]

写法就三种：`#pnote(note: […])[…]` 是普通旁注，`#pnote-warn(…)` 是提醒（橙色），`#pnote-tip(…)` 是技巧（青色）。文字颜色在 `note.typ` 的 `note-style` 里调。行级的 `#mnote` 另有 `mnote-warn` / `mnote-tip` 两个偏应用。

要针对某一句话而不是整段，写成 `#mnote`：这条旁注的落点就是这里#mnote[看，它跟着我所在的这一行走，不是跟着整段的顶边]，同页多条还会自动上下避让。整段的裁剪与补充仍用 `#pnote`——它不依赖外部包，也更稳。

== 提示框

#pnote(note: [这一条规矩是从书籍样板继承的：早先每个类型的框都上饱和度色，一页排六个框就是一页彩虹，正文反倒被压住了。])[
  提示框与书籍样板是同一套：框身共用一种极浅的中性底，颜色只出现在左上角挂着的标题牌上，类型靠图标和文字区分。
]

#keypoint-box[
  要点框适合放「一句话记牢」的东西。图标是灯泡，橙色。
]

#pitfall-box[
  易错框适合放踩过的坑。笔记里最有价值的部分往往是这一类内容：正着走的路到处都有，摔过的那一下只有自己知道。
]

#conclusion-box[结论框适合放一段推演之后落地的判断。]
#formula-box[公式框适合放需要反复查的表达式。]
#reference-box[参考框适合放延伸阅读、相关笔记、外部链接。]

代码框默认允许跨页（长代码不用手动加 `breakable: true`）：

#code-box[
  ```python
  def fib(n):
      return n if n < 2 else fib(n - 1) + fib(n - 2)
  ```
]

要自定义就用 `#callout(title: [], icon: "", color: rgb("#…"))[…]`，图标名见 Heroicons 官网。框默认整体不跨页，内容确实很长时加一句 `breakable: true`。

== 表格与代码

表格的列默认左对齐，需要居中的列在各自的 `table` 里单独指定——不统一的话，同一份笔记里的几张表会各用一套规矩：

#figure(
  table(
    columns: (auto, auto, 1fr),
    table.header([元素], [写法], [用途]),
    [旁注], [`#pnote(note: […])[…]`], [正文之外的补充、疑问、反对],
    [提示框], [`#keypoint-box[…]`], [要点、易错、结论、公式、参考、代码六类],
    [编号环境], [`#theorem[…]`], [定义、定理、引理、推论、命题、例，可交叉引用],
  ),
  caption: [模板提供的版式元素一览],
) <tab:note-elements>

代码块里的中文必须显式补中文字体回退，否则 Typst 会在系统字体里乱挑（实测会落到隶书）。模板已经处理好了：

```typst
#set page(paper: "a4")
#let x = 1 + 1
```

操作步骤写成有序列表，编号自动维持：

+ 写内容，`typst compile single.typ 笔记.pdf` 看效果；
+ 改 `note.typ` 顶部的版式参数调版面；
+ 内容攒多了，改用 `collection.typ` 编成一册。

== 公式与编号环境

行内公式如 $e^(i pi) + 1 = 0$，行间公式：

$ integral_0^oo (sin x)/x dif x = pi/2 $

需要被反复引用的是定义与定理。它们的编号在每篇笔记内从 1 重新开始，用 `@标签` 的写法交叉引用：

#definition[
  若两条直线相交成直角，则称它们*互相垂直*。
]<def:perpendicular>

#theorem[（勾股定理）直角三角形两直角边的平方和等于斜边的平方。]<thm:pythagoras>

#proof[
  设两直角边为 $a, b$，斜边为 $c$。把四个全等的直角三角形拼成边长为 $a + b$ 的正方形，中间留出的正方形面积为 $c^2$；用同一块面积作另一种计算，得 $(a+b)^2 - 2 a b = c^2$，整理即 $a^2 + b^2 = c^2$。
]

#pnote(note: [模板在每篇笔记开头把编号归零，所以两篇笔记里都会出现「定理 1」——这是有意的，笔记各自成篇。])[
  @def:perpendicular 与 @thm:pythagoras 的编号由模板维护（引用自带「定义」「定理」前缀，不要再手写一遍），插入或删除一个环境，后续编号与全文引用会一起更新。
]

插图沿用书籍样板的 `figstyle.typ`：示意图、流程图、数据图共用同一套颜色、字体与线宽。@fig:note-flow 是流程图（Fletcher 绘制）：

#figure(
  flow(
    node((0, 0), [写笔记]),
    node((1, 0), [编译]),
    node((2, 0), [看图调整]),
    edge((0, 0), (1, 0), "->", label: [`.typ`]),
    edge((1, 0), (2, 0), "->", label: [PDF]),
    edge((2, 0), (0, 0), "-->", label: [改版式], bend: -30deg),
  ),
  caption: [写笔记的循环：内容和版式一起长],
) <fig:note-flow>

== 两个出口

同一篇笔记文件可以被两个入口使用，内容一份都不用改：

- `single.typ` — 单篇出口，一篇笔记一个 PDF，改一下 include 的那一行即可；
- `collection.typ` — 汇总出口，首页是标题加总目录（自动生成、页码可点），之后每篇各起一页，续页带书眉和页码。

汇总册的总目录只依赖各级标题，所以增删笔记只要改 `collection.typ` 里的 include 列表，目录和页码会自己跟上。
