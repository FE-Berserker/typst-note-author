// 示例笔记之二：一篇以旁注为主的笔记，同时也是模板设计取舍的说明。
#import "../note.typ": *

#note-header(
  title: "这套笔记模板为什么这样排",
  date: "2026-09-20",
  tags: ("Typst", "排版", "设计"),
  status: "已定稿",
  summary: "把书籍样板搬到笔记上时做的几个取舍：砍掉什么、保留什么、为什么。改版式之前先看这篇，能省掉一次推翻重来。",
  // 这篇固定用青灰色：它讲的是模板自身，颜色被后来的笔记借去也不合适
  theme: note-themes.slate,
)

== 为什么不直接沿用书籍模板

#pnote(note: [书籍样板里最费工的三样东西——切口色标、篇章页、装订边距——全都建立在「双面印刷 + 装订」这个前提上。单面打印时它们会集体错位：宽边跑到左边，色标贴着订口，篇章页后面多出一张空白衬页。])[
  书籍样板的骨架是为「装订成册、一次成稿、给他人读」设计的，笔记是「单面阅读、持续增补、给自己看」。两者的差别不是尺寸，是假设。
]

#figure(
  table(
    columns: (1fr, 1.15fr),
    table.header([书籍样板的机制], [笔记模板的处理]),
    [切口色标（按章次递增的色块）], [砍掉。它要按物理页码手工维护空白衬页清单，而笔记增删频繁，清单永远在过期],
    [篇章页 + 篇幅小目录], [砍掉。笔记不平，一篇就是一篇],
    [装订边距 + 奇数页起章], [改成单面：左边固定窄、右边固定宽，宽边让给旁注],
    [章号空心大字], [砍掉。笔记头已经足够醒目，再放大一个数字是重复],
    [提示框、图形样式、中文字体规则], [原样继承。这三样与「书」无关，是纯排版资产],
  ),
  caption: [书籍机制在笔记里的去留],
) <tab:keep-or-drop>

== 为什么右侧要空出一条栏

#pnote-tip(note: [代价是版心窄了约三成。若某篇笔记确实不需要旁注，把 `note-note-width` 调小、`pnote` 就不再用，正文会自动占满。])[
  因为笔记里同时有几种声音。正文说的是「这件事是怎样的」，旁注说的是「我为什么信」「哪里存疑」「和什么有关」。后者一旦塞进正文，视线就会被反复打断；单独成一栏，正文可以一口气读下去。
]

#pnote-warn(note: [这条限制只对 `place` 成立，值得记住：下次想「让某段文字跟到某一行的位置」时，别再从 `place` 试起。])[
  实现方式值得记一笔：最早旁注想用 `place` 从句子外面推，而不是用 `grid` 并排。原因是 Typst 的 `place` 写在段落里时，定位基准是外层容器而不是当前行——实测它会落在整页版心的垂直中心，同一页上几条旁注全部叠在一处，把内容从块级的 `block` 换成行内的 `box` 也一样。
]

#pnote(note: [旁注顶边对齐段落顶边，所以一条旁注概括一整段最自然。])[
  换成 `grid` 之后，旁注锚在段落上而不是句子上。这在当时是个取舍：注解本来就多半针对一整段，为它付出的代价只是「要对准某个句子时把段落拆短」。
]

后来 `marginalia` 用另一条路补上了「锚在位置上」——它不靠 `place`，而是用 state 记录每条旁注已经占了哪一段垂直空间，排版时互相推开。这条旁注里就有它：#mnote[你现在读的这句话，落点就是这句话所在的位置，不是这一段的顶边。]所以现在两种各就各位：针对某一句话的即时反应用 `#mnote`，整段的裁剪与补充用 `#pnote`。前者每来一条就多一轮 state 循环，整篇都用它反而更慢——这也是它只当「可选升级」、没有替换掉 `pnote` 的原因。

== 为什么默认单面

#pnote(note: [左边 2.0 cm 是翻阅的余量，右边 5.0 cm 里 3.4 cm 是旁注栏、0.6 cm 是正文与旁注之间的气口、1.0 cm 是纸边。换开本时改 `note-paper`，右边距自动跟着走。])[
  打印笔记的现实是这样的：多半单面打印，或者干脆只在屏幕上看。书籍的订口 / 切口不对称边距在这种场景下完全是负担——装订线那一侧翻到偶数页就跑到了右边，宽边白留在错误的一侧。
]

#keypoint-box[
  书是装订的，笔记是散页的。凡是依赖「左右页不同」的版式——不对称边距、奇偶书眉、切口色标——在笔记里都会失效，除非你真的去装订。
]

因此模板采用固定的左窄右宽，页码一律贴右，不做奇偶区分。

== 每篇一种主题色

汇合成册之后，最先遇到的问题不是字号而是「我现在翻到哪了」。一本书靠章号，笔记没有章号；靠标题，标题在书眉里已经有一个了。所以给每一篇一种主题色：短标、节标题竖标、摘要竖线、旁注竖线四处同色，翻页时颜色先认出来，标题是第二眼的事。

颜色按篇序从色板里顺序取，第一篇总是主题红——所以单独导出一篇时，观感和以前没有任何区别。新建一篇不用写任何东西，拿到的就是没用过的下一种；这篇显式写了 `theme: note-themes.slate`，#mnote[就是这一篇：颜色被钉死了，以后往册子中间插笔记，它的青色不会跟着别人后移。]是想固定某篇颜色时的写法。

代价说在前面：*往中间插一篇，后面各篇的颜色会整体后移一位*。换来的一定不撞车。色板只有六种，第七篇起开始第二轮，此时同色相隔六篇，不容易混。黑白打印时不指望它——六种亮度里有两对只差一档灰，分不出，那时认标题。

== 两件事留给了「以后」

- *打印稿的两个开关*。`note-hide-notes` 隐藏全部旁注、`note-monochrome` 换成灰阶配色，都是改一行重编译的事。想一条命令出两种 PDF（屏幕版与复习版）就得外面套个脚本，或者每次手改这一行。
- *交叉引用*。`@fig:…` 这类引用在单篇里够用；跨篇引用分两个出口：汇总册里标签全文可见，`@sec:…` 能直接引用另一篇的节（印出来是那节的标题）；单篇导出只包含这一篇，引用没被 include 的标签会直接编译报错，这时才用 `#link("另一篇.pdf")[…]` 手工链。

#pnote-tip(note: [模板的价值在于「写的时候不用想版式」，不在于覆盖所有情况。缺什么再加，加之前先问一句：这一条是不是也能用一行 `set` 规则解决。])[
  写到这里就够了——笔记不必像书那样把每件事都做完整。
]
